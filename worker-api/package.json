{
  "name": "worker-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ../wrangler.jsonc",
    "build": "wrangler deploy --dry-run --config ../wrangler.jsonc --outdir ../dist/worker-api",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@bea-uptime/contracts": "workspace:*",
    "hono": "^4.12.9",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250321.0",
    "typescript": "^5.8.3"
  }
}
